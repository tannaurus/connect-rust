// Stub crate body — this exists only so cargo metadata succeeds when
// crates_universe resolves the dependency graph from Cargo.toml. The actual
// Rust library lives at src/lib.rs and is built by Bazel.
